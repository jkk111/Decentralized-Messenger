module.exports = function() {
  /*
    Logic behind adding friends:
    ClientA sends a request to clientB with public key.
    request is queued for ClientB.
    ClientB accepts, sends public key.
    ClientA receives public key and can now send messages.
  */
}
